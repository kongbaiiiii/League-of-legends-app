package interface_adapter.select_stat;

import interface_adapter.apply_stats.ApplyStatsViewModel;

public class SelectStatPresenter {
    private final SelectStatViewModel selectStatViewModel;

    public SelectStatPresenter(SelectStatViewModel selectStatViewModel) {
        this.selectStatViewModel = selectStatViewModel;


    }
}
